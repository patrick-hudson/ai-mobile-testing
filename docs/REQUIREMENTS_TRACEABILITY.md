# Requirements traceability

This ledger prevents requirements from disappearing as the suite evolves.

| Requirement | Implementation | Verification |
| --- | --- | --- |
| Compare old and redesigned environments | paired environment/project metadata and production route mapping | paired smoke/release projects and checklist environment badges |
| Audit one deployment without a comparison origin | discriminated `single-site` run contract, explicit Preview/Production role, neutral targets, standalone Product Oracles, and separate durable Docker execution/finalization pools | `site-preflight:self-test`, `run-compiler:self-test`, `single-site-launch:self-test`, `single-site-command:self-test`, `single-site-execution:self-test`, and `single-site:pools:self-test` |
| Preview scope before creating a Single-site run | side-effect-free quitting7oh identity/revision preflight plus definition/case/target coverage compilation; launch atomically repeats both and rejects stale preview | `site-preflight:self-test`, `route-inventory:self-test`, `live-route-inventory:self-test`, and `single-site-launch:self-test` |
| Full and targeted Single-site scope remain honest | canonical `FULL` only for the complete versioned profile; plugin/audit/area/target subsets remain `TARGETED`; immutable coverage manifest records selected, omitted, outside-mode, and gap reasons | `run-compiler:self-test`, `single-site-enumeration:self-test`, report self-test, and portal launch/report journeys |
| One-origin tests use independent expectations | definition classifications `standalone-compatible`, `standalone-required`, and `comparison-only`; case `supportedModes` and `oracleVariants`; comparison-only migration/parity checks excluded instead of pointed twice at one origin | `plugins:metadata-self-test`, `assertions:self-test`, `assertions:ledger:check`, and `run-compiler:self-test` |
| Single-site execution is durable and worker-scalable | named-volume queue, leases, fencing tokens, bounded retries, up to 16 worker replicas, one separate finalizer, no Docker socket, and sealed attempt artifacts | `single-site-queue:self-test`, `single-site-worker:self-test`, `single-site:pools:self-test`, and `single-site-execution:self-test`; accepted limitation: run exactly one finalizer because active/active finalizer leasing is not implemented |
| Single-site intra-job parallelism is bounded honestly | one worker owns one fenced job and may use the configured Playwright worker count; Docker worker replicas increase throughput across independent queued jobs but do not divide one run into independently fenced shards | Accepted limitation: a full Single-site job is not distributed across worker containers. `single-site:pools:self-test` proves cross-job claim isolation; comparative release remains the supported sharded coordinator when one run must be split across containers. |
| Single-site media and reports cannot mutate sealed source evidence | contained regular-file processed copy, digest/byte/count verification, verbose FFmpeg stage, immutable media manifest, crash-safe report/gallery publications bound to the sealed source digest | `single-site-media:self-test`, `single-site-report-input:self-test`, `single-site-finalization-reader:self-test`, and `single-site-gallery-publication:self-test` |
| Single-site truth dimensions do not collapse into one green status | advisory `HEALTHY`/`FINDINGS`/`INCOMPLETE` Site Health, independent `COMPLETE`/`GAPS`/`UNKNOWN` Coverage, manual status, Visual Review, Evidence Authority, and Pipeline Integrity | `site-health:self-test`, `single-site-report:self-test`, finalization reader self-test, and portal report journeys |
| Same-site visual drift has an explicit baseline lifecycle | exact baseline identity and rendering fingerprint, approve/replace/revoke/delete mutations, immutable provenance/history, purge-independent copied media, generated `UNCHANGED`/`CHANGED` plus absent/incompatible/unavailable states, Finding-waiver rationale that cannot erase a Finding, and append-only human `REVIEWED` dispositions bound to an exact comparison and baseline revision | `visual-baseline:self-test`, `portal-visual-baseline-api:self-test`, `single-site-visual-comparisons:self-test`, `single-site-visual-review:self-test`, and Single-site gallery journey |
| Preview certificate exception is bounded and truthfully downgraded | strict default and baked Netskope CA; `preview-bypass` requires confirmed Preview role plus exact `AUDIT_PREVIEW_TLS_BYPASS_ALLOWLIST` origin; Production and comparative runs stay strict; bypass makes Evidence Authority non-authoritative | `tls:check`, `site-preflight:self-test`, `single-site-launch:self-test`, and portal security acceptance |
| Reclaim Single-site storage without deleting active baselines | terminal-only typed purge, queue/finalization containment and symlink/mount guards, mutation fence, durable journal/quarantine recovery, independent baseline bytes retained | `single-site-purge:self-test` and portal Single-site purge journey |
| Make Single-site retention explicit | manual terminal-run purge plus explicit baseline revoke/media-delete operations; no implicit baseline advancement or hidden cleanup | purge and visual-baseline self-tests; accepted limitation: automatic age-based retention and garbage collection are not implemented |
| Single-site AI cannot gate or mutate deterministic/human truth | per-run opt-in, finalized mode-aware allowlisted packet, `interpret-health-evidence` capability only, prohibited mutation/output fields, independent retryable state | `ai-mode-aware:self-test`, `single-site-ai-review:self-test`, AI API security tests, and portal report journey |
| Test every published feature and route | explicit 102-route inventory, 81 feature/cross-cutting contracts, and 102 generated route contracts | inventory contract plus 183 authoritative checklist rows |
| Assertions prove product behavior | generated promise-to-oracle ledger plus a fail-closed declaration parser that rejects tautologies, swallowed failures, conditional-only checks, observation-only cases, and missing executable cases | `assertions:self-test`, mutation canaries, `assertions:ledger:check`, and focused Docker product checks |
| Find visual redesign defects | matched viewport captures, strict visual snapshots, all-route top/middle/bottom evidence, distributed long-page baselines, overflow/layout checks | videos, screenshots, diffs, page geometry |
| Videos with full context | explicit interaction policy plus structured audit attachment and reporter evidence gate; range-independent luma, initial/final content, and post-content action probes reject blank/static helpers; narrowly eligible leading capture gaps become new content-hashed derivatives that must pass the full gate again | checklist row links, video manifest normalization record, poster, checksum, synthetic mutations, and recovered canonical-run clip proof |
| Non-technical per-run report | compact report data, paged audit API, lazy evidence/detail/log loading | portal reviewer report, release summary, filters, manual and AI sections |
| Large-run browser safety | capped compact JSON layers, incremental artifact index, abortable requests, bounded DOM pages and 64 KiB log preview | portal acceptance suite verifies the raw manifest/log are not fetched eagerly |
| Run-wide visual review | one revisioned logical-media catalog, shared portal/archive workbench, queue/overview modes, suite grouping, filters, context, flags, and keyboard/focus contract | Docker gallery journeys, immutable checklist gallery, direct-file acceptance, saved portal/archive captures |
| Exact reference scale | 5,659 artifacts, 1,241 logical items, 110 validated videos, 17,527 physically written/recounted corpus files; 256 KiB chunks, 512 KiB details, 100-row pages | `gallery-scale:self-test` and canonical `portal:e2e:scale` raw metrics/materialization counts |
| Gallery responsiveness | first-usable mark before deferred thumbnails; independent abort generations; virtualized queue/overview; one selected video; byte-aware retained revision cache | 5+30 cold and 10+100 warm Docker samples, p95 thresholds, ≤500 DOM, ≤25 MiB heap, 50-change stale-work proof |
| Portable archived review | embedded pinned descriptor plus source/token/revision-validated one-shot opaque-origin iframe wrappers; CORS/frame policy is limited to generated archive modules and revision wrappers; published-order fast path; contained evidence URLs | normal no-flag `file://` and HTTP browser acceptance, ≤3 cold wrappers, zero leaked iframes, read-only capture, and mutation-origin rejection |
| Reclaim obsolete run storage | terminal-state-only purge API and typed confirmation; direct-child, real-path, symlink, nested-mount guards; root-only durable journal and atomic quarantine before deletion | Docker portal acceptance plus restart recovery canaries purge isolated portal/external fixtures, reject unsafe targets, and recover interrupted deletion as retryable `evidence-failed` truth |
| Long build checklist | catalog includes automated and manual requirements even when unexecuted | searchable offline checklist with NOT RUN/MANUAL states |
| Portal launches targeted tests | allowlisted profile/project/plugin/area/audit selection | portal API validation and end-to-end Docker portal run |
| Launch capacity survives initialization faults | append streams must emit `open` before registration/spawn; the pre-await reservation stays held through persisted registration; stream/persistence/spawn setup errors become terminal retained records or are removed if storage is unavailable | isolated injected stream-open, persistence, and spawn failures are purgeable and do not block the next launch |
| Portal evidence cannot impersonate canonical signoff | explicit single-container review-only provenance; any review reason downgrades a `READY` checklist to `review-required`; report hero shows signoff withheld | pure READY/TLS/flaky/reduced/canonical self-test plus synthetic READY reviewer-report journey |
| Local portal trust boundary | request-wide loopback/allowed Host validation plus a root-derived operator capability and same-origin browser mutations; workers receive neither capability nor session | hostile Host/Origin, missing-capability, origin-less worker, sandboxed-origin, and operator-authorized API regressions across credential/run/stop/purge/manual routes |
| Manual evidence integrity | per-run upload/attestation/rebuild mutex plus signature, FFprobe stream, and FFmpeg first-frame validation with cleanup | concurrent upload/attestation API checks and fake-PNG rejection/no-file regression |
| Durable artifact delivery | contained real path plus opened no-follow descriptor, handled stream lifecycle, and byte ranges | outside-run symlink, disappeared file, playable HTTP 206 video checks |
| Bounded restart recovery | known external reads use stale-while-revalidate; each refresh has a 2 MiB/250 ms global budget and active progress comes from a bounded heartbeat sidecar rather than a log-prefix scan | security self-test materializes an 8 MB-class cold log, verifies prompt parallel reads, exact restored progress, skipped-prefix accounting, and the global byte ceiling |
| Purge closes every observer | one purge event closes/clears run and gallery SSE clients and heartbeat timers | Docker acceptance observes purge on both streams before evidence disappears |
| Track run start through finish | persisted manifest, phases, timestamps, SSE replay/reconnect, stop state | refresh/reconnect portal acceptance test |
| Slow portal work stays understandable | asynchronous requests, busy regions, skeletons/spinners, disabled duplicate actions, reduced-motion support | delayed-response and interaction acceptance checks |
| Verbose useful logs | commands, stages, test progress, steps, HTTP responses, errors, timings, exits | persisted `logs/runner.log` and visible portal stream |
| Docker portability and Mac stability | pinned official Playwright Linux image and Compose services | image build, container smoke, portal-run evidence |
| Fast runs without invalid performance regressions | eight one-worker functional Docker shards by default, followed by a dedicated one-worker Lighthouse container; every fresh blob merges into one decision; the coordinator atomically reserves a new run directory and refuses prior evidence | default-alignment canary, exact 1,337 + 70 discovery partition, stale/missing blob and existing-run refusal self-tests, dynamic portal shard visibility |
| External lifecycle truth cannot contradict itself | portal derives terminal state from `pipeline.status === completed`, `pipeline.completed === true`, fully normalized checklist release fields (`ready`, reason, blocker counts, integrity), and the matching declared lifecycle status; contradictions become `evidence-failed` with reported fields retained as diagnostics | external discovery rejects both `ready`/running/`NOT_READY` and completed/`READY`-with-blockers fixtures, then recovers only after a fully consistent rewrite |
| Netskope and development TLS | baked public CA and strict default; comparative bypass rejected; exact-origin, confirmed-Preview Single-site bypass recorded as non-authoritative | strict container requests, `tls:check`, and Single-site preflight/launch bypass canaries |
| FFmpeg visibility and video processing | post-run poster/checksum manifest with command logging | `video-manifest.json`, poster files, stage log |
| AI evidence review | optional Anthropic post-run analyzer with secret-safe runtime injection | AI JSON/HTML output, API status/latency/usage logs, no-key dry run |
| Save Claude key in portal | encrypted Docker credential vault with replace/delete and fingerprint-only state | save/restart/delete acceptance using a synthetic key plus plaintext leakage scan |
| Browser/report workers cannot exfiltrate Claude secrets | root supervisor owns a mode-0700 project-scoped vault outside `/work`; Playwright/FFmpeg use `pwuser`, AI uses `aiworker`, and checklist generation uses `reportworker`; none can read the vault; the key crosses one anonymous stdin pipe; report sources are frozen and report output is privately staged then atomically published; evidence inputs require contained no-symlink/no-follow reads; saved credentials are disabled without all worker identities | kernel-level vault denial for all three worker UIDs, absolute-outside/symlink/hard-link/destination canaries, child-environment/secret-pipe assertions, Docker targeted-run acceptance, and credential lifecycle tests |
| AI cannot hide deterministic failures | advisory label and reporter release policy | checklist retains original test disposition |
| Extensible tests | validated installed Playwright/TypeScript plugin manifests | validation command, first-party plugins, disabled starter |
| Ship plugins for redesigned site | platform/content, UX, calculators, meetings, accessibility/performance/reliability plugins | portal discovery and plugin selection |
| CI | Docker smoke/release workflows and retained artifacts | workflow configuration plus container commands |
| Physical-device honesty | manual real-device and screen-reader audit rows | checklist remains incomplete until human evidence is attached |
| Expandable browser/device coverage | validated target registry with seven stable defaults, opt-in iOS/Android emulations, capability-gated branded Edge, and non-runnable provider metadata for real devices | `targets:self-test`, configuration fail-closed checks, and recorded project/browser labels |
